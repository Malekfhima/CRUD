{
  "name": "rn-web-express-server",
  "version": "1.0.0",
  "description": "Backend API for Item CRUD used by Expo React Native web client",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "cross-env NODE_ENV=development nodemon server.js",
    "test": "jest --runInBand"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "axios": "^1.12.2",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.2",
    "mongoose": "^7.0.0"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "jest": "^29.0.0",
    "mongodb-memory-server": "^8.12.1",
    "nodemon": "^2.0.22",
    "supertest": "^6.3.3"
  }
}
